// src/components/Aurora.native.js
// Fallback untuk native — WebGL tidak tersedia di native
// Hanya return null, hero tetap pakai background warna solid

export default function Aurora() {
  return null;
}